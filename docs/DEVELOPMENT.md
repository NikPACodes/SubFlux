# Development Guide

Документ описывает порядок развертывания проекта SubFlux в локальном окружении и базовые правила разработки.

Этот файл предназначен для разработчиков и контрибьюторов.

---

## Оглавление
- [Development Guide](#development-guide)
  - [Оглавление](#оглавление)
  - [Требования к окружению](#требования-к-окружению)
  - [Локальная установка](#локальная-установка)
    - [1. Клонирование репозитория](#1-клонирование-репозитория)
    - [2. Создание виртуального окружения](#2-создание-виртуального-окружения)
    - [3. Установка зависимостей](#3-установка-зависимостей)
  - [Настройка переменных окружения](#настройка-переменных-окружения)
    - [1. Создание .env файла](#1-создание-env-файла)
    - [2. Минимально необходимые переменные](#2-минимально-необходимые-переменные)
    - [Опциональные переменные](#опциональные-переменные)
  - [Работа с базой данных](#работа-с-базой-данных)
    - [1. Использование установленной PostgreSQL](#1-использование-установленной-postgresql)
    - [2. Использование PostgreSQL через Docker](#2-использование-postgresql-через-docker)
  - [Запуск тестов](#запуск-тестов)
  - [Запуск проекта](#запуск-проекта)
  - [Работа с миграциями](#работа-с-миграциями)
  - [Pull Request и процесс разработки](#pull-request-и-процесс-разработки)
  - [Принципы разработки](#принципы-разработки)
  - [Частые проблемы](#частые-проблемы)
  - [Дополнительная документация](#дополнительная-документация)

---

## Требования к окружению
- __Python 3.12+__  
  Требуется для __Django 6.0+__. Версии ниже 3.12 не поддерживаются.
- __PostgreSQL 14+__ или (__Docker__)
- __Git__
- (опционально) __Redis__  
  Используется для Celery, фоновых задач и кэширования.
- (опционально) __Docker__ + __Docker Compose__

Проверка версии Python:
```
 python --version 
```

---

## Локальная установка

### 1. Клонирование репозитория
```
git clone https://github.com/NikPACodes/SubFlux.git
cd SubFlux
```

### 2. Создание виртуального окружения
```
python -m venv .venv
source .venv/bin/activate        # Linux / macOS
```

### 3. Установка зависимостей
```
pip install -r requirements.txt
```

---

## Настройка переменных окружения

### 1. Создание .env файла
```
cp .env.example .env
```

### 2. Минимально необходимые переменные
```
SECRET_KEY = '********'
POSTGRES_NAME     = '********'
POSTGRES_USER     = '********'
POSTGRES_PASSWORD = '********'
POSTGRES_HOST     = '********'
POSTGRES_PORT     = '********'
```

### Опциональные переменные
```
REDIS_URL             = '********'
CELERY_BROKER_URL     = '********'
CELERY_RESULT_BACKEND = '********'
```
---

## Работа с базой данных

### 1. Использование установленной PostgreSQL
#### Применение миграций
```
python manage.py migrate
```

#### Создание суперпользователя
```
python manage.py createsuperuser
```

### 2. Использование PostgreSQL через Docker
Этот способ рекомендуется, если PostgreSQL не установлен локально 
или требуется воспроизводимое окружение.

#### Запуск БД
В корне проекта, выполнить команду:
```
docker compose -f docker/postgres.yml up -d
```

Проверка статуса контейнера:
```
docker ps
```

#### Остановка базы
```
docker compose -f docker/postgres.yml down
```

#### Полная очистка БД (⚠️ __ОСТОРОЖНО__)
Удаляет volume с данными:
```
docker compose -f docker/postgres.yml down -v
```

#### Применение миграций
```
python manage.py migrate
```

#### Создание суперпользователя
```
python manage.py createsuperuser
```

---

## Запуск тестов
Полный прогон тестов:
```
pytest
```

Запуск конкретного приложения:
```
pytest apps/subscriptions/
```

___CI (GitHub Actions)___ _автоматически запускает тесты при создание Pull Request._

---

## Запуск проекта
```
python manage.py runserver
```
__Сервер будет доступен по адресу:__
`http://127.0.0.1:8000/`

---

## Работа с миграциями
- Миграции должны быть зафиксированы в репозитории.
- Не добавляйте папки migrations в .gitignore.
- После изменения моделей:
```
python manage.py makemigrations
python manage.py migrate
```

---

## Pull Request и процесс разработки
### Общие правила
- Один Pull Request — одна логическая задача.
- Изменения бизнес-логики желательно сопровождать тестами.
- Не добавляйте секреты и конфиденциальные данные в репозиторий.
- Прямые коммиты в master запрещены — используйте Pull Request.

### Проверки
Перед созданием PR:
```
pytest
```

_CI должен быть зелёным перед merge._

---

## Принципы разработки

- Бизнес-логика размещается в service layer, а не в serializers или views.
- Изменение цены никогда не переписывает историю.
- Инварианты данных защищаются:
  - сервисным слоем,
  - ограничениями базы данных.
- Любые критические изменения должны сопровождаться тестами.
- Не допускается прямое изменение данных, минуя доменные сервисы.

---

## Частые проблемы

### Несоответствие версии Python
Убедитесь, что используется __Python версии 3.12 или выше__.

### Ошибка подключения к базе данных
Проверьте корректность переменных окружения в __.env__.

### Ошибки после обновления кода
Выполните:
```
python manage.py migrate
```

---

## Дополнительная документация

- [ARCHITECTURE](./ARCHITECTURE.md) — описание архитектуры проекта  
- [ROADMAP](./ROADMAP.md) — текущее состояние проекта и дальнейшие этапы развития